var React = require('react');
var ReactDOM = require('react-dom');
var bootstrap = require('react-bootstrap');
var Form = require('./components/Form.jsx');

ReactDOM.render(<Form/>, document.getElementById('validationForm'));
